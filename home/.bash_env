# ~/.bash_env — environment variables only

# Always point BASH_ENV to this file so subshells and scripts get it too
export BASH_ENV="$HOME/.bash_env"

# Path to your dotfiles repo
export DOTFILES="$HOME/.dotfiles"

# Path modifications
export PATH="$HOME/bin:$PATH"
export PATH="/home/eyong/repos/ninja/build-cmake:$PATH"

# Editor and terminal defaults
export EDITOR=nvim
export VISUAL=nvim
export TMUX_CONF="$HOME/.config/tmux/tmux.conf"
# Homebrew environment (safe for non-interactive shells too)
if [ -f /home/linuxbrew/.linuxbrew/bin/brew ]; then
  eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"
  export HOMEBREW_NO_ENV_HINTS=1
fi
#
# Prefer a forwarded agent if it works; otherwise fall back to a stable local socket
if ! SSH_AUTH_SOCK="${SSH_AUTH_SOCK:-}" ssh-add -l >/dev/null 2>&1; then
  export SSH_AUTH_SOCK="${SSH_AUTH_SOCK:-$HOME/.ssh/agent.sock}"
fi
